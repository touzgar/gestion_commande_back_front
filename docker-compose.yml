

services:
  backend:
    build: ./backend
    container_name: gestion_backend
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: gestion_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
